# deploy

Placeholder for the hosted (Streamable HTTP) deployment — deliberately empty in the MVP.
The local stdio mode ships first; the remote mode adds an HTTP transport entry in
`packages/server/src/transports/` plus a Dockerfile and manifests here, with no changes to the
protocol or distribution layers.
